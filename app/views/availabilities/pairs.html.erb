<h1>Available pairs</h1>

<p>
    You are available to pair from <%=h @availability.start_time %> to <%=h @availability.end_time %>
</p>

<p>
  The following devs are available to pair with you at this time:
</p>

<table class="pairs">
  <tr>
    <th>Developer</th>
    <th>Start time</th>
    <th>End time</th>
    <th>Dev time</th>
    <th>Contact</th>
  </tr>

<% @possible_pairs.each do |pair| %>
  <tr>
    <td><%=h pair.dev2 %></td>
    <td><%=h pair.start_time %></td>
    <td><%=h pair.end_time %></td>
    <td><%=h ((pair.end_time - pair.start_time) / 3600).floor %>h<%=h (((pair.end_time - pair.start_time) % 3600) / 60).to_i %>m</td>
    <td><%=h pair.contact %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Make yourself available to pair', new_availability_path %>